/* 
 * Author: Ori Popowski/Vladimir Moki
 */

/* 
 * `pass2' runs through `instructions' and then through `data'. It does:
 * 1. Writes the `.ob' file.
 * 2. Writes the `.ext' file.
 */
void pass2(void);
